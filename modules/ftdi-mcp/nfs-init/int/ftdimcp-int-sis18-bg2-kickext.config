# hacky reset all USB ports after reboot
# (SL7?)
#sleep 5
#for i in /sys/bus/pci/drivers/[uoex]hci_hcd/*:*; do
#  [ -e "$i" ] || continue
#  echo "${i##*/}" > "${i%/*}/unbind"
#  echo "${i##*/}" > "${i%/*}/bind"
#done
#sleep 5
